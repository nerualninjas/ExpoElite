import React from 'react';

const ProductPage = () => {
    return (
        <div>
            {/* make a modal for add product  */}
            <button>Add New Product</button>
        </div>
    );
};

export default ProductPage;